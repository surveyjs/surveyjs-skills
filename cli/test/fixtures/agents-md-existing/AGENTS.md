# Contributor guide for agents

## Build

Run `npm run build` before opening a pull request.

## Style

- Two-space indentation, no semicolon-free style.
- Keep modules under 300 lines.

## Testing

`npm test` runs the unit suite. Integration tests need a local Postgres on port 5432.
